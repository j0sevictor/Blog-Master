<nav class="navbar navbar-expand-lg bg-dark" data-bs-theme="dark">
  <div class="container-fluid">
    <span class="navbar-brand mb-0 h1">Blog Master</span>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon">Menu</span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link" href="/">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" aria-current="page" href="/categorias">Categorias</a>
        </li>
      </ul>
    </div>
    {{#if user}}
      {{#each user}}
        {{#if nome}}
          <span class="navbar-brand">{{nome}}</span>
          <a href="/usuario/logout"><button class="btn btn-outline-secondary me-2" type="button">Logout</button></a>
        {{/if}}
      {{/each}}
    {{else}}
      <form class="d-flex">
        <a href="/usuario/login"><button class="btn btn-outline-success me-2" type="button">Login</button></a>
        <a href="/usuario/registro"><button class="btn btn-outline-secondary" type="button">Registrar-se</button></a>
      </form>
    {{/if}}
  </div>
</nav>